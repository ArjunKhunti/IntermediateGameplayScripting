using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TernaryExpression : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int num = 5;
        string msg;
        msg = num > 3 ? "Greater than 3" : "Less than 3";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
